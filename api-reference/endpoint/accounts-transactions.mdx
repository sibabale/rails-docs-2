---
title: "Get Account Transactions"
api: "GET https://api.rails.com/api/v1/accounts/{account_id}/transactions"
description: "Retrieve transaction history for a specific account"
---

Retrieves the transaction history for a specific account.

## Path Parameters

<ParamField path="account_id" type="string" required>
  Unique identifier for the account (UUID format)
</ParamField>

## Query Parameters

<ParamField query="limit" type="number">
  Maximum number of transactions to return (default: 50)
</ParamField>

## Response

Returns an array of transaction objects.

<ResponseField name="id" type="string">
  Unique transaction identifier (UUID)
</ResponseField>

<ResponseField name="account_id" type="string">
  Account ID associated with the transaction
</ResponseField>

<ResponseField name="transaction_type" type="string">
  Type of transaction: `deposit`, `withdrawal`, or `transfer`
</ResponseField>

<ResponseField name="amount" type="number">
  Transaction amount (decimal)
</ResponseField>

<ResponseField name="currency" type="string">
  Currency code (e.g., `USD`)
</ResponseField>

<ResponseField name="description" type="string">
  Transaction description (if provided)
</ResponseField>

<ResponseField name="status" type="string">
  Transaction status: `completed`, `pending`, or `failed`
</ResponseField>

<ResponseField name="created_at" type="string">
  ISO 8601 timestamp of transaction creation
</ResponseField>

<ResponseField name="updated_at" type="string">
  ISO 8601 timestamp of last update
</ResponseField>

## Example

<RequestExample>

```typescript TypeScript SDK
import { Rails } from "@rails/sdk";

const rails = new Rails({
  apiKey: process.env.RAILS_API_KEY
});

const transactions = await rails.accounts.getTransactions("123e4567-e89b-12d3-a456-426614174000", {
  limit: 25
});

console.log("Transactions:", transactions.length);
transactions.forEach(txn => {
  console.log(`${txn.transactionType}: $${txn.amount} - ${txn.description}`);
});
```

```typescript Fetch API
const response = await fetch('https://api.rails.com/api/v1/accounts/123e4567-e89b-12d3-a456-426614174000/transactions?limit=25', {
  headers: {
    'Authorization': 'Bearer your_api_key_here',
    'Content-Type': 'application/json'
  }
});

const transactions = await response.json();
```

</RequestExample>

<ResponseExample>

```json Success Response (200 OK)
[
  {
    "id": "abc12345-e89b-12d3-a456-426614174000",
    "account_id": "123e4567-e89b-12d3-a456-426614174000",
    "transaction_type": "deposit",
    "amount": 500.00,
    "currency": "USD",
    "description": "Salary deposit",
    "status": "completed",
    "created_at": "2024-01-15T15:20:00Z",
    "updated_at": "2024-01-15T15:20:00Z"
  },
  {
    "id": "def67890-e89b-12d3-a456-426614174000",
    "account_id": "123e4567-e89b-12d3-a456-426614174000",
    "transaction_type": "withdrawal",
    "amount": 100.00,
    "currency": "USD",
    "description": "ATM withdrawal",
    "status": "completed",
    "created_at": "2024-01-15T16:30:00Z",
    "updated_at": "2024-01-15T16:30:00Z"
  },
  {
    "id": "ghi12345-e89b-12d3-a456-426614174000",
    "account_id": "123e4567-e89b-12d3-a456-426614174000",
    "transaction_type": "transfer",
    "amount": 250.00,
    "currency": "USD",
    "description": "Transfer to savings",
    "status": "completed",
    "created_at": "2024-01-15T17:00:00Z",
    "updated_at": "2024-01-15T17:00:00Z"
  }
]
```

```json Error Response (404 Not Found)
{
  "error": {
    "code": "account_not_found",
    "message": "No account found with the provided ID"
  }
}
```

</ResponseExample>

## Error Codes

| Code | Description |
|------|-------------|
| `account_not_found` | Account ID does not exist |
| `unauthorized` | Invalid or missing API key |
| `invalid_uuid` | Invalid UUID format for account ID |