---
title: "List Accounts"
api: "GET https://api.rails.com/api/v1/accounts"
description: "List accounts filtered by user ID"
---

Retrieves all accounts for a specific user. The `user_id` query parameter is required.

## Query Parameters

<ParamField query="user_id" type="string" required>
  User ID to filter accounts (UUID format)
</ParamField>

## Response

Returns an array of account objects.

<ResponseField name="id" type="string">
  Unique identifier for the account (UUID)
</ResponseField>

<ResponseField name="account_number" type="string">
  Auto-generated account number
</ResponseField>

<ResponseField name="account_type" type="string">
  Type of account: `checking` or `saving`
</ResponseField>

<ResponseField name="user_id" type="string">
  ID of the account owner
</ResponseField>

<ResponseField name="balance" type="number">
  Current account balance (decimal)
</ResponseField>

<ResponseField name="currency" type="string">
  Currency code (e.g., `USD`)
</ResponseField>

<ResponseField name="status" type="string">
  Account status: `active`, `suspended`, or `closed`
</ResponseField>

<ResponseField name="organization_id" type="string">
  Organization ID (if applicable)
</ResponseField>

<ResponseField name="environment" type="string">
  Environment: `production` or `sandbox`
</ResponseField>

<ResponseField name="created_at" type="string">
  ISO 8601 timestamp of account creation
</ResponseField>

<ResponseField name="updated_at" type="string">
  ISO 8601 timestamp of last update
</ResponseField>

## Example

<RequestExample>

```typescript TypeScript SDK
import { Rails } from "@rails/sdk";

const rails = new Rails({
  apiKey: process.env.RAILS_API_KEY
});

const accounts = await rails.accounts.list({
  userId: "456e7890-e89b-12d3-a456-426614174000"
});

console.log("User has", accounts.length, "accounts");
accounts.forEach(account => {
  console.log(`${account.accountType}: $${account.balance}`);
});
```

```typescript Fetch API
const response = await fetch('https://api.rails.com/api/v1/accounts?user_id=456e7890-e89b-12d3-a456-426614174000', {
  headers: {
    'Authorization': 'Bearer your_api_key_here',
    'Content-Type': 'application/json'
  }
});

const accounts = await response.json();
```

</RequestExample>

<ResponseExample>

```json Success Response (200 OK)
[
  {
    "id": "123e4567-e89b-12d3-a456-426614174000",
    "account_number": "ACC001234567",
    "account_type": "checking",
    "user_id": "456e7890-e89b-12d3-a456-426614174000",
    "balance": 1250.75,
    "currency": "USD",
    "status": "active",
    "organization_id": null,
    "environment": "production",
    "created_at": "2024-01-15T10:35:00Z",
    "updated_at": "2024-01-15T15:20:00Z"
  },
  {
    "id": "789e1234-e89b-12d3-a456-426614174000",
    "account_number": "ACC001234568",
    "account_type": "saving",
    "user_id": "456e7890-e89b-12d3-a456-426614174000",
    "balance": 5000.00,
    "currency": "USD",
    "status": "active",
    "organization_id": null,
    "environment": "production",
    "created_at": "2024-01-15T10:36:00Z",
    "updated_at": "2024-01-15T15:20:00Z"
  }
]
```

```json Error Response (400 Bad Request)
{
  "error": {
    "code": "validation_error",
    "message": "user_id query parameter is required"
  }
}
```

</ResponseExample>

## Error Codes

| Code | Description |
|------|-------------|
| `validation_error` | Missing or invalid `user_id` query parameter |
| `unauthorized` | Invalid or missing API key |